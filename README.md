# AWAQ Backend (REST, Express + TypeScript)

- Run dev: `npm install && npm run dev`
- Health: `GET /health`
- API base: `/api/v1` (common/abt/agromo/robot)

See `docs/` for architecture and ops. Contracts in `src/contracts/openapi`.
