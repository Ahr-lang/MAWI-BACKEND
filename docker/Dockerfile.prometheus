FROM prom/prometheus:latest

# Copy the prometheus configuration into the image
COPY observability/prometheus.yml /etc/prometheus/prometheus.yml

# Expose Prometheus port
EXPOSE 9090

# Use the default prometheus command with our config
CMD ["--config.file=/etc/prometheus/prometheus.yml", "--storage.tsdb.retention.time=30d"]
